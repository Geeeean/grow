import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/(app)/_layout/certifications')({
    component: () => <div>Hello /(app)/_layout/certifications!</div>,
});
